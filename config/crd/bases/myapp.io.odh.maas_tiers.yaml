---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: tiers.myapp.io.odh.maas
spec:
  group: myapp.io.odh.maas
  names:
    kind: Tier
    listKind: TierList
    plural: tiers
    singular: tier
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tier is the Schema for the tiers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TierSpec defines the desired state of Tier.
            properties:
              models:
                description: |-
                  Models that this tier applies to
                  If empty, applies to all models. If specified, only these models are affected.
                items:
                  type: string
                type: array
              rateLimits:
                description: Rate limits for requests (HTTP requests per time window)
                properties:
                  counters:
                    description: |-
                      Counter expressions for rate limit tracking
                      Default: ["auth.identity.userid"]
                    items:
                      type: string
                    type: array
                  limit:
                    description: Maximum number of requests allowed
                    format: int32
                    type: integer
                  window:
                    description: Time window for the rate limit (e.g., "2m", "1h",
                      "30s")
                    type: string
                required:
                - limit
                - window
                type: object
              targetRef:
                description: TargetRef references the MaasPlatform this tier applies
                  to
                properties:
                  name:
                    description: Name of the MaasPlatform resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the MaasPlatform resource
                      If empty, defaults to the same namespace as the Tier resource
                    type: string
                required:
                - name
                type: object
              tokenRateLimits:
                description: Token rate limits (tokens per time window from model
                  responses)
                properties:
                  counters:
                    description: |-
                      Counter expressions for token rate limit tracking
                      Default: ["auth.identity.userid"]
                    items:
                      type: string
                    type: array
                  limit:
                    description: Maximum number of tokens allowed
                    format: int32
                    type: integer
                  window:
                    description: Time window for the token rate limit (e.g., "1m",
                      "1h", "30s")
                    type: string
                required:
                - limit
                - window
                type: object
            required:
            - targetRef
            type: object
          status:
            description: TierStatus defines the observed state of Tier.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
